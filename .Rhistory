cat("\n===== ANÁLISE DA LARANJA =====\n")
setwd("E:/Projects/FarmTech")
print(paste("Média da Área:", media_area))
cat("🌱 FARM TECH - Análise Estatística\n")
cat("====================================\n\n")
# Verificar diretório de trabalho atual
cat("📂 Diretório atual:", getwd(), "\n")
cat("\nAnálise concluída com sucesso!\n")
print(paste("Média da Área (Laranja):", media_area_laranja))
# Calcular desvio padrão da área em m²
desvio_area_laranja <- sd(laranja$area_m2, na.rm = TRUE)
desvio_area <- sd(cana$Área,   na.rm = TRUE)
if ("area_m2" %in% names(laranja)) {
media_area_laranja  <- mean(laranja$area_m2, na.rm = TRUE)
desvio_area_laranja <- sd(laranja$area_m2,   na.rm = TRUE)
cat(sprintf("Média da Área (Laranja): %g\n", media_area_laranja))
cat(sprintf("Desvio padrão da Área (Laranja): %g\n", desvio_area_laranja))
}
## ====== Ajustar colunas ======
if ("Área" %in% names(cana)) {
cana$Área <- num_silencioso(cana$Área)
} else if ("Area" %in% names(cana)) {
cana$Area <- num_silencioso(cana$Area)
}
# Seleciona apenas colunas numéricas
colunas_numericas <- sapply(dados, is.numeric)
